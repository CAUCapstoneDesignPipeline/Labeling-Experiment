# 실험 0 — 사전 측정 프로토콜

캡스톤디자인1 / 최종 갱신 2026-09-06

본 실험(실험 A)의 파라미터를 확정하기 위한 소규모 사전 측정.
**결과를 내는 실험이 아니라 본 실험의 다이얼을 맞추는 작업이다.**

---

## 0. 사전 등록 원칙

아래 기준은 **데이터를 보기 전에** 확정하고 이후 변경하지 않는다.
변경할 경우 변경 사실과 사유를 보고서에 남긴다.

이 문서와 두 가이드는 라벨링 시작 **전에** 저장소에 커밋한다.
커밋 시각이 "결과를 보고 기준을 맞춘 것이 아니다"의 증거가 된다.

### 고정 상수 (팀 합의로 확정)

| 기호 | 값 | 의미 |
|---|---|---|
| `n` | 100 | 본 실험 이슈 수 |
| `c` | 0.70 | 목표 PR 커버리지 |
| `κ_gate` | 0.61 | 일치도 게이트 |
| `κ_exp` | 0.70 | 표본 계산용 예상 κ |
| `M_req` | 150 | κ₂ 추정에 필요한 Normative 유닛 수 |

### 게이트

| 지표 | 기준 |
|---|---|
| Fleiss κ₁ (Normative/Descriptive) | 95% CI 하한 > 0.61 |
| Fleiss κ₂ (E/S/L) | 95% CI 하한 > 0.61 |
| E 비율 | 95% CI 하한 > 임계값 `p*` |

판정은 3분기로 한다. 점추정치만 임계값과 비교하지 않는다.

- CI 하한 > 기준 → **성립**
- CI 상한 < 기준 → **불성립**
- 기준이 CI 안 → **판정 유보**, 표본 추가

---

## 1. 임계값·표본 크기의 근거

이 분야에 표준 임계값은 없다. 아래 방식으로 유도하고 과정을 보고서에 싣는다.

### 1.1 E 비율 임계값 — 효용 기반

사용자가 체감하는 값은 문장 단위 비율이 아니라 "내 PR 리포트에 [검증됨] 항목이
하나라도 뜨는가"이다. 요구사항 수 k, 문장별 E 확률 p일 때

```
PR 커버리지 = 1 - (1-p)^k
p* = 1 - (1-c)^(1/k)          k = m̄_norm
```

| m̄_norm (=k) | c=0.70 → p* |
|---|---|
| 2 | 45.2% |
| 3 | 33.1% |
| 4 | 26.0% |
| 5 | 21.4% |

**사전 등록 대상은 임계값 숫자가 아니라 목표 커버리지 `c`=0.70이다.**
k는 실험 0에서 측정하고, 그 값으로 `p*`를 확정한 뒤 본 라벨링에 들어간다.
이 순서가 "데이터를 보고 숫자를 골랐다"를 배제한다.

### 1.2 E+S 임계값 50% — 베이스라인 비교

이 도구가 없을 때의 대안은 LLM이 PR 전체를 읽고 판정하는 것이며, 파일럿에서
오염률 47.0% / 비변별률 59~74%로 측정됐다. 리포트 항목의 과반이 L 등급이면
도구는 그 베이스라인으로 퇴화한다. E+S ≥ 50%는 "베이스라인과 구분되는 최소
조건"으로 서술한다.

세 근거 중 가장 약하므로 **보조 지표로만 쓴다.** 주 판정은 1.1의 E 비율.

### 1.3 문헌 앵커 — 새너티 체크

E는 기대 동작이 이슈에 명시되어야 성립한다. 그 충족률의 기존 측정치.

| 출처 | 값 | 단위 |
|---|---|---|
| Chaparro et al. | 버그 리포트의 약 65%가 기대 동작 누락 → 명시 약 35% | 리포트 |
| 웹앱 이슈 태깅 연구 (코멘트 포함) | 재현 절차·결과 정보 각각 50% 초과 | 리포트 |
| Yakusu (Android) | 리포트에서 실행 가능한 테스트 생성 성공 59.7% | 리포트 |
| PROMISE_exp | 969건 중 기능 요구사항 444건 (약 46%) — 느슨한 상한 | SRS 문장 |

**임계값이 아니라 새너티 체크다.** 측정값이 30~50%면 문헌과 정합적이고,
5%나 90%면 임계값 판정에 앞서 **라벨링 규칙부터 의심한다.**

전부 버그 리포트 대상이라는 점에 주의. 대상 저장소는 라이브러리라
enhancement 비중이 높고, enhancement는 원하는 동작을 정의하는 것이 목적이므로
더 높게 나올 수 있다. 집계 시 bug/enhancement를 나눠 보고한다.

### 1.4 표본 크기

제약이 둘이고 **단위가 다르다.**

| 제약 | 단위 | 필요량 | 결과 |
|---|---|---|---|
| E 비율 추정 | 이슈 | n = 100 | CI ±9.4%p (p=0.35) |
| κ₂ 추정 | Normative 유닛 | M_req ≈ 150 | CI ±0.10 (κ=0.70 가정) |

`n = z²p(1-p)/e²`. p=0.35에서 ±10%p에 87건, ±8%p에 137건.
n=100은 임계값 26%와의 거리(약 9%p)를 판정할 수 있는 최소선이다.

M_req=150은 "게이트 0.61 + 예상 κ 0.70 → CI 하한이 0.61을 넘으려면"에서 역산한
값이다. `SE(κ) ≈ sqrt(P_o(1-P_o) / (n(1-P_e)²))`.

| 실제 κ | 0.61 초과 확정에 필요한 유닛 수 |
|---|---|
| 0.80 | 약 40 |
| 0.75 | 약 75 |
| 0.70 | 약 150 |
| 0.65 | 약 600 |

**κ가 0.65면 학기 내 확정이 불가능하다.** 그래서 실험 0에서 잠정 κ를 먼저 본다.

```
n_ov = ceil(M_req / m̄_norm)      상한 100
```

| m̄_norm | n_ov | p* (c=0.70) |
|---|---|---|
| 2 | 75 | 45.2% |
| 3 | 50 | 33.1% |
| 4 | 38 | 26.0% |
| 5 | 30 | 21.4% |

**m̄_norm 하나가 n_ov와 p*를 동시에 결정한다.** m̄_norm이 작으면 라벨링 부담과
통과 기준이 함께 불리해지므로, 그런 결과가 나오면 분리 규칙부터 재확인한다.
1.5 미만이면 100건 전부를 3인 중복해도 150유닛에 못 미치므로 이슈를 늘린다.

---

## 2. 표본

### 2.1 대상 저장소

`google/guava`, `eclipse-vertx/vert.x`.
둘 다 라이브러리/프레임워크이며, **업무 로직 중심 애플리케이션으로의 일반화는
확인하지 못했다.** 보고서에 한계로 명시한다.

### 2.2 수집 조건 (`collect_issues.py`)

포함 — 전부 만족

1. `is:issue is:closed`
2. 머지된 PR과 연결 (GraphQL `closedByPullRequestsReferences`)
3. `created:>=2020-01-01`
4. 영어 본문, 20단어 이상
5. 연결 PR이 main 소스의 `.java`를 1개 이상 수정

제외 — 위 조건에서 자동 탈락.
추출은 **시드 고정 무작위**이며 정렬 기반이 아니다. 정렬은 인프라·보안 이슈로
표본을 편향시킨다.

텍스트 범위는 이슈 제목 + 본문 + **연결 PR이 열리기 이전 코멘트**까지.
도구가 실제 운영 시점에 접근 가능한 범위와 일치시킨 것이다.

### 2.3 실측 결과 (2026-09-06)

```
guava    조회 3054 → 통과  34
vert.x   조회 2870 → 통과 153
전체 풀 187건
```

| | exp0 | main |
|---|---|---|
| 건수 | 10 | 100 |
| 시드 | 20260905 | 20260906 |

**두 표본은 겹치지 않는다.** 같은 표본으로 임계값을 정하고 그 임계값으로
판정하면 순환이 되므로, exp0 10건을 먼저 뽑고 나머지에서 100건을 뽑는다.

저장소 구성비는 guava 18% / vert.x 82%. 사실상 vert.x 중심 실험이며,
저장소별 분리 보고는 검정력이 부족하므로 하지 않는다.

### 2.4 부수 집계

| 항목 | 값 | 용도 |
|---|---|---|
| 이슈당 연결 PR 수 | guava 1.03 / vert.x 1.01 | 이슈:PR = 1:1 가정 검증 |
| PR → 이슈 연결률 | guava 4.7% / vert.x 8.4% | **제품 적용 범위** |

연결률은 최근 1000건, closing 키워드(`Fixes #N`) 기준이다. 본문에 `#N`만 언급한
PR은 포함되지 않으므로 실제 적용 범위는 이보다 넓을 수 있다. 보고 시 조건을
명시한다.

guava는 날짜 컷오프 통과분 213건 중 165건(77%)이 머지 PR과 연결되지 않는다.
Google 내부에서 개발하고 GitHub으로 동기화하는 구조 때문이며, **수집 실패가
아니라 도구가 적용되지 않는 개발 워크플로우가 실재한다는 증거**로 쓴다.

---

## 3. 유닛 분리 (`split.py`)

### 3.1 규칙

전처리 — 삭제하지 않고 토큰으로 치환한다. 삭제하면 문장 구조가 깨진다.

```
펜스 코드 블록 / 스택 트레이스   ⟦CODE:n⟧   → 첨부로 분리
인라인 코드                      ⟦C:n⟧      → 최종 텍스트에 복원
버전 번호                        ⟦V:n⟧      → 최종 텍스트에 복원
URL                              ⟦URL⟧
HTML 태그 / 마크다운 강조         제거
```

인라인 코드 보호가 HTML 제거보다 **먼저** 실행된다. 순서가 바뀌면
`Supplier<SslContextFactory>` 같은 Java 제네릭이 태그로 오인되어 잘린다.

블록 단위

| 블록 | 처리 |
|---|---|
| 이슈 제목 | **유닛으로 포함** — 요구사항이 제목에만 압축된 경우가 많다 |
| 본문 마크다운 헤딩 | 유닛 아님 (템플릿 구조) |
| `**Expected:**` 등 볼드 헤더 | 블록 경계로 취급, 유닛 아님 |
| 불릿 / 번호 목록 | **항목 하나 = 유닛 하나** |
| 표 | 행 하나 = 유닛 하나 |
| 인용 | 같은 이슈에 원본이 있으면 제외, 없으면 유지 |
| 산문 문단 | 문장 분리 (축약어·버전 예외 처리) |

후처리 — 3단어 미만 유닛은 직전에 병합하되 **세그먼트(제목/본문/코멘트n)
경계를 넘지 않는다.** 넘으면 다른 사람의 코멘트가 앞에 붙는다.

템플릿 잔재 제거 — `_No response_`, 체크박스, GitHub 알림 메일 답장
보일러플레이트, 버전만 적힌 필드.

### 3.2 실측 결과

```
이슈 10건 → 유닛 147개
m̄_all = 14.7   (최소 2 / 중앙 7.5 / 최대 47)
```

**분포가 치우쳐 있다.** guava 2건(#8158 47유닛, #5773 41유닛)이 전체의 60%를
차지하며, 각각 붙여넣은 LLM 답변과 잡담이 대부분이다.

평균과 중앙값이 2배 차이이므로, m̄_norm 추정도 불안정할 수 있다.
파라미터 계산 시 **중앙값 기준으로도 계산해 비교**하고, 차이가 크면 보수적인
쪽(작은 m̄ → 큰 n_ov)을 택한다.

### 3.3 units.jsonl은 배포 후 재생성하지 않는다

`split.py`를 다시 돌리면 `sent_id` 번호가 밀려 이미 붙인 라벨과 어긋난다.
분리 규칙 개정은 **라벨링 시작 전에** 끝내고 전체를 재분리한다.

### 3.4 프로덕션과의 관계

`split.py`는 실험 전용이 아니라 파이프라인이 그대로 쓸 모듈이다.
문장 분리를 LLM에 맡기지 않기로 했으므로, 실험과 프로덕션이 같은 코드를 쓰면
이 단계는 별도 검증이 필요 없다.

---

## 4. 라벨링

### 4.1 실행

```bash
python3 label.py --dir data/exp0 --labeler A
```

3인이 `units.jsonl`을 공유하고 각자 `labels_<식별자>.jsonl`에 기록한다.
판정 기준은 두 가이드에만 있으며, 도구는 실행 시점에 가이드 파일을 읽는다.

### 4.2 반드시 지킬 것

- **라벨링 중 팀원과 상의하지 않는다.** 독립성이 깨지면 일치도 측정이
  무의미해지고 실험을 다시 해야 한다. 질문은 note에 적고 전원 완료 후 회의에서
  처리한다.
- **전원이 끝나기 전에는 서로의 라벨 파일을 열지 않는다.** `.gitignore`가
  `labels_*.jsonl`을 막고 있다.
- **연결된 PR의 diff를 보지 않는다.** 유닛 파일에도 diff를 넣지 않았다.
- **이슈에 없는 기대 동작을 채워 넣지 않는다.** 파일럿에서 관측된 오염과 같은
  오류이며 측정값을 무효화한다.
- 애매하면 낮은 쪽으로 내린다.

### 4.3 2단계 대상 — 교집합

κ₂는 **3인 전원이 Normative로 판정한 유닛**에서만 계산한다.

1단계에서 판정이 갈리면 2단계 대상 집합이 라벨러마다 달라져 Fleiss κ를 계산할
수 없다. 교집합을 쓰는 이유는 κ₁이 이미 1단계 불일치를 측정하고 있어,
κ₂까지 그것을 떠안으면 같은 문제를 두 번 세게 되기 때문이다.

대가 두 가지를 보고서에 적는다.

- 표본이 줄어든다. **m̄_norm도 교집합 기준으로 측정한다.** 개인 평균으로 재면
  실제보다 크게 나와 n_ov가 과소 산정된다.
- κ₂가 낙관적으로 나온다. 애매한 유닛이 1단계에서 이미 걸러지기 때문이다.

### 4.4 합의 라벨

실험 B의 정답 기준은 합의 라벨이며, 3인 일치 → 그대로 / 2:1 → 다수결 /
전원 불일치 → 회의 합의, 실패 시 제외로 만든다.

**κ는 합의 전 원본 라벨로 계산한다.** 합의 후 라벨로 계산하면 일치도가
인위적으로 올라간다.

---

## 5. 측정 정의

```
M_all(10)        10건에서 나온 전체 유닛 수          = 147 (실측)
m̄_all            M_all / 10                          = 14.7 (실측)

N_A, N_B, N_C    라벨러별 Normative 유닛 수
개인 m̄_norm       N_x / 10                            (참고값)
N_∩              3인 전원 Normative 유닛 수
교집합 m̄_norm     N_∩ / 10                            ← 파라미터 계산에 쓰는 값

n_ov = ceil(150 / 교집합 m̄_norm)          상한 100
p*   = 1 - (1 - 0.70)^(1 / 교집합 m̄_norm)
```

개인 평균과 교집합의 격차를 반드시 함께 기록한다.
격차가 크면 1단계 가이드부터 손봐야 한다는 신호다.

유닛당 라벨링 소요 시간은 `labels_*.jsonl`의 `ts` 필드로 계산한다.
본 실험 작업량 = `(n_ov × 3 + (100 − n_ov)) × m̄_norm × 유닛당 초`.

---

## 6. 잠정 kappa와 판정

κ₁은 전체 유닛(147개) 대상, κ₂는 교집합 대상.
Fleiss를 대표값으로, pairwise Cohen을 진단용으로 병기한다.

```python
from statsmodels.stats.inter_rater import aggregate_raters, fleiss_kappa
from sklearn.metrics import cohen_kappa_score
from itertools import combinations

table, _ = aggregate_raters(labels)      # (n_units, 3)
print("Fleiss:", fleiss_kappa(table))
for i, j in combinations(range(3), 2):
    print(f"{i}-{j}:", cohen_kappa_score(labels[:, i], labels[:, j]))
```

`statsmodels`는 Fleiss의 CI를 주지 않는다. 부트스트랩으로 뽑되
**문장이 아니라 이슈 단위로 리샘플링**한다. 유닛은 이슈 안에서 군집되므로
문장 단위로 뽑으면 CI가 실제보다 좁게 나온다.

### 판정

10건 규모에서 κ₂의 CI는 매우 넓다. **판정용이 아니라 아래 갈래를 고르는
신호로만 쓴다.**

| 잠정 κ₂ | 조치 |
|---|---|
| ≥ 0.75 | n_ov 축소 검토 후 진행 |
| 0.65 ~ 0.75 | 계획대로 진행 |
| < 0.65 | **가이드 개정.** 표본 확대가 아니다 |

마지막 줄의 근거는 1.4의 표다. 참값 κ가 0.65면 게이트 초과 확정에 약 600유닛이
필요해 학기 내 불가능하다. κ가 낮다는 것은 정밀도 문제가 아니라 가이드가
애매하다는 신호다.

**잠정 κ 확인이 파라미터 계산보다 먼저다.** 못 믿을 라벨로 센 m̄_norm으로
파라미터를 뽑으면 그 파라미터도 못 믿는다.

### kappa 역설 주의

Descriptive가 70%를 넘는 쏠린 분포가 예상된다. 이 경우 단순 일치율이 85%여도
kappa가 0.4대로 내려갈 수 있다. 가이드가 나쁜 것이 아니라 지표의 성질이다.

**일치율·kappa·주변분포를 항상 함께 보고**해야 원인을 구분할 수 있다.

```
κ 낮음 + 분포 고름  → 가이드가 애매함  → 가이드 개정
κ 낮음 + 분포 쏠림  → 지표의 성질      → 일치율 병기로 설명
```

---

## 7. 실행 순서와 진행 상황

```
[✓] 1. collect_issues.py --stage pool      풀 187건
[✓] 2. --stage sample                      exp0 10 / main 100, 비중복 확인
[✓] 3. --stage prstats                     PR→이슈 연결률
[✓] 4. split.py --dir data/exp0            유닛 147개
[✓] 5. review.md 검수 → 규칙 3회 개정 후 확정
[✓] 6. 저장소 커밋 (라벨링 전 = 사전 등록)
[ ] 7. 3인 독립 라벨링                      ← 여기
[ ] 8. κ₁, κ₂ 계산 → 판정 (§6)
[ ] 9. 교집합 m̄_norm → n_ov, p* 확정
[ ] 10. 본 실험 착수
```

7번부터 3인이 동시에 필요하다. 147유닛에 1인당 1~2시간.

---

## 8. 결과 기록표

```
수집 풀 크기          187
시드 (exp0 / main)    20260905 / 20260906
M_all(10)             147        m̄_all         14.7
N_A / N_B / N_C       ___        개인 m̄_norm    ___
N_∩                   ___        교집합 m̄_norm  ___    ← 격차: ___%

κ₁ Fleiss  ___   CI [___, ___]   P_o ___   분포 N:D ___
κ₂ Fleiss  ___   CI [___, ___]   P_o ___   분포 E:S:L ___
pairwise   AB ___   AC ___   BC ___

유닛당 라벨링 시간    ___ 초
bug / enhancement 별 Normative 비율   ___ / ___

→ n_ov = ___      p* = ___
```

---

## 9. 알려진 한계

보고서에 그대로 옮긴다.

- 대상이 라이브러리 2개뿐이며, 업무 로직 중심 애플리케이션으로의 일반화는
  확인하지 못했다.
- 표본의 82%가 vert.x다. 사실상 단일 저장소 실험에 가깝다.
- "머지된 PR과 연결됨" 조건은 **결국 수정된 이슈만** 남긴다. 모호해서 방치된
  이슈는 표본에서 빠지므로 실제 E 비율은 측정값보다 낮을 가능성이 있다.
- 컷오프를 2020년으로 완화해 2020~2022년 이슈가 섞였다. 이슈 작성 관행이
  시기별로 다를 수 있으므로, 연도별 E 비율을 나눠 확인한다.
- PR→이슈 연결률은 closing 키워드 기준이며 최근 1000건만 집계했다.
- 교집합 방식 때문에 κ₂는 낙관적으로 나온다(§4.3).
- 실험 A가 측정하는 것은 "어떤 방법으로 검증 **가능한가**"이며, 실제 검증
  결과(E3/E2/E1/E0)가 아니다. 측정된 E 비율은 **상한**이다.
